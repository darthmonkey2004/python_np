#!/usr/bin/env python3

from np

isrunning = np.shell("pgrep np")
if isrunning != '':
	#If running, assume gui open is needed.
	with open(np.COMFILE, "w") as f:
		f.write("create_gui")
	f.close()
else:
	pypath = f"{np.EXEC_DIR}/core/np.py"
	np.python(pypath)
